# Copy to .env locally if you use an environment loader.
# Never commit real credentials or private infrastructure endpoints.

DEEPSEEK_API_KEY=replace-with-your-api-key
DEEPSEEK_BASE_URL=https://your-api-endpoint.example
DEEPSEEK_MODEL=your-model-name
